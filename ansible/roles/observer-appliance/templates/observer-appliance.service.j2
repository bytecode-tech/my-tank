[Unit]
Description=Observer Appliance
After=network.target

[Service]
User={{ observer_user }}
WorkingDirectory={{ observer_basedir }}
ExecStart=/usr/local/bin/uwsgi --ini observer.ini

[Install]
WantedBy=multi-user.target